apiVersion: v1
kind: Service
metadata:
  name: dev-mcp-server-duckdb
  namespace: biodiversity
spec:
  selector:
    app: dev-duckdb-mcp
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
