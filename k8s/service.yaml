apiVersion: v1
kind: Service
metadata:
  name: mcp-data-server
spec:
  selector:
    app: mcp-data-server
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 3600
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8080
